import { type FC } from "react";

interface FiltersProps {}

const Filters: FC<FiltersProps> = ({}) => {
  return (
    <div>
      <div className="w-full bg-white ">this is the filters component</div>
    </div>
  );
};

export default Filters;
